import { DataTerminalCustomPipe } from './data-terminal-custom.pipe';

describe('DataTerminalCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new DataTerminalCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
